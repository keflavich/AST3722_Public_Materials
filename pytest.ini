[pytest]
testpaths = tests
# Notebooks that hit SESAME/SIMBAD/VizieR/SkyView are skipped by default; run
# them deliberately with `pytest -m network`.
addopts = -m "not network" -ra --strict-markers
markers =
    network: notebook queries a remote astronomical service
filterwarnings =
    ignore::DeprecationWarning
